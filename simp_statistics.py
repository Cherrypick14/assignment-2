#   simple stats on a list of values

import numpy as np

listing = [1,2,3,4,5]

print("average: ", np.average(listing))
print("variance: ", np.var(listing))
print("mean: ", np.mean(listing))
print("median: ", np.median(listing))

